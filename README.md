# Front-end for MiCasa

## Setup Instructions

Istall project dependencies with:

`npm install`


Build project with:

`npm run build`

Output will be in dist/ directory


To start build on watch mode (rebuild on file change):

`npm run dev`
